<%= form_with(model: catagory, class: "contents") do |form| %>
    <% if catagory.errors.any? %>
      <div id="error_explanation" class="msg-error">
        <h2> There are: <%= pluralize(catagory.errors.count, "error") %>: </h2>
  
        <ul>
          <% catagory.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: "" %>
    </div>
  
    <div class="field">
      <%= form.label :icon %>
      <%= form.text_field :icon, class: "" %>
    </div>
  
    <div class="inline">
    </div>
  
    <div class="">
      <%= form.submit "#{action_name == 'edit' ? 'Update' : 'Save'}", class: "" %>
    </div>
  <% end %>